apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchman.fullname" . }}
  labels:
    {{- include "watchman.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "watchman.selectorLabels" . | nindent 4 }}
